# Vitis Scaffold

The V1 app is a Linux user-space controller for:

- USB capture;
- DMA buffer allocation;
- HLS AXI-Lite configuration;
- depth/point cloud result readback.

Scripts target XSCT/Vitis 2021.2 and expect `xsa/v1_usb_ps_hls.xsa` after Vivado export.

